数据库管理规范

表命名应根据表的用途和类型进行统一分类命名，例如档案表、业务日志表、系统日志表、数据关系表等。命名原则示例：
- 使用语义前缀或后缀表明类型：archive_/dim_（档案、维表），biz_log_/event_log_（业务日志），sys_log_（系统日志），rel_/link_（关系表）。
- 保持小写、用下划线分隔，避免使用中文或空格，保证可读性与一致性。
- 名称应包含主体和用途，必要时包含时间粒度或分区信息。
示例：archive_customer、biz_log_order_event、sys_log_auth、rel_order_product。